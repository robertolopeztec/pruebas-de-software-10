# Actividad 4.2. Ejercicio de programación 


## Consideraciones
- `test.sh` es un script de bash que ayuda a validar cada uno de los scripts sobre sus directorios de prueba `../test`
- Dentro del subdirectorio `results` se encuentra:
  - Los resultados para scripts que requieren escribir en un archivo.
  - Los resultados de haber aplicado `pylint` sobre el script (`pylint-result.txt`)
  - Los resultados del script `test.sh` (`results.txt`)